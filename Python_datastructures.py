#***Strings***
#### Week 1 -Assignment 6.5 ###
###6.5 Write code using find() and string slicing (see section 6.10) to extract the number at the end of the line below.
# Convert the extracted value to a floating point number and print it out.

#*** text = "X-DSPAM-Confidence:    0.8475";
# x=text.find(':')
# #print(x)
# y=text[x+1:]
# print(float(y))
# #print(y)

#***Files***
### Week 2 - Assignment 7.1###
###7.1 Write a program that prompts for a file name, then opens that file and reads through the file,
# and print the contents of the file in upper case. Use the file words.txt to produce the output below.
# Use words.txt as the file name

###Save Words.txt file in the same directory###
#***fname = input("Enter file name: ",)
# fh = open(fname)
# for i in fh:
#     i = i.rstrip().upper() ##desired output is in capitals##
#     print(i)
